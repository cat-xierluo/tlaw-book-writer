# 变更日志 — tlaw-book-writer

## [1.0.0] - 2026-09-20

### 新增

- **书稿写手核心流程**：输入章节号 + 素材 → 备料三读（规范 / 配置口径 / 范文）→ 写作（骨架=节说明、格式=规范、风格=范文指纹）→ 事实边界（不编造、占位标注）→ 内置自查 → 交付三通道（手动粘贴默认 / 他人代转 / lark-cli 写入〔须先真机验证〕）。
- **首次使用引导**：配置未初始化时问答采集（署名 + 在写章节）；连接飞书 CLI 的可从多维表格书稿计划读取并与用户二次确认后写入本地配置。
- **`references/standards-checklist.md`**：写作规范的可执行检查表（结构版——检查项与方法齐备，具体参数值由配置的规范文档提供）。
- **`references/feishu-cli-cookbook.md`**：飞书 CLI 手册（连接上手 / 活体读取 / 规范更新哨兵 / 文档创建与块写入 / 图片上传；各节标注验证状态）。
- **`config/project-profile.example.yaml`**：配置模板（零真实 token；复制为本地实例使用，.gitignore 排除）。
- 技能上下文文件：CHANGELOG / DECISIONS / TASKS。
