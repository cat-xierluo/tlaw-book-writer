# Decisions — tlaw-book-writer

> 记录技能的设计取舍。

## [DEC-001] - 2026-09-20 - 定位：驱动 agent 写稿，不生产给人看的指引

- **背景**：早期形态偏预检/指引生成，实际使用者是写稿人的 agent 而非人。
- **决策**：输入章节号 + 素材，agent 备料三读（规范 / 配置口径 / 范文）后直接成稿，交稿前内置自查；不产出指引类中间文档。
- **影响**：SKILL 主体为"输入→三读→写作→事实边界→自查→交付"流。

## [DEC-002] - 2026-09-20 - lark-cli 为可选增强，写入须先真机验证

- **背景**：飞书 CLI 读取链路（节点解析 / 全文拉取 / revision 查询）验证可用；写入链路（md 导入任务 / 逐块建文）未验证，不宜直接在正式文档上试错。
- **决策**：lark-cli 定位可选增强——活体校验（含规范更新哨兵）与交付通道 c；未安装/未授权时静默降级到手动粘贴（通道 a 默认）。写入首用前须以测试文档走通并删除；API 只用于读和建自己的文档。
- **影响**：无 CLI 环境不影响核心功能；cookbook 各节标注验证状态。

## [DEC-003] - 2026-09-20 - 配置双层：example 模板零真实值，真实配置走本地实例

- **背景**：配置含环境与个人信息（文档路径 / 飞书 token / 哨兵基线 / 署名）。
- **决策**：`config/project-profile.example.yaml` 为纯模板（占位或结构示例）；使用时复制为 `project-profile.yaml` 本地实例（.gitignore 排除）。哨兵基线为 null 时自动跳过。
- **影响**：模板可安全分发。

## [DEC-004] - 2026-09-20 - 事实边界：不编造，占位标注

- **决策**：素材里没有的法条条号、案号、数字、产品功能一律不落为事实——写"【需核实：…】"占位并继续其余部分；示例材料用虚构或公开来源并明标。

## [DEC-005] - 2026-09-20 - 风格基线由配套材料提供，提取报告与范稿不入本仓

- **背景**：各书的写作规范全文、范稿与提取报告属该书私有内容，不宜随技能仓分发。
- **决策**：SKILL 对提取报告的引用改为**配置驱动**（`profile.extraction_report`；null 时降级为仅读范稿原文自行提炼）；范稿路径同样由 `profile.sample_docs` 配置。
- **影响**：本仓 references/ 仅含 checklist 与 CLI 手册。

## [DEC-006] - 2026-09-20 - 交付三通道（手动粘贴默认）

- **决策**：a 手动粘贴进知识库（默认、零依赖）；b 他人代转；c lark-cli 写入（须先真机验证）。三通道收尾动作一致（图片原图另存、进度更新、评论处理）。
