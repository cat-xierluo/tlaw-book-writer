---
name: tlaw-book-writer
description: 书稿写手（TLAW）——按配置的范稿风格与写作规范，直接完成章节写作。作者给出章节号与素材（可多可少），agent 备料三读（规范/配置口径/范文格式与风格）后成稿，交稿前内置自查。说"写这一节/起草 X.X/按样文风格写"时使用。不生产给人看的写作指引，不做质量深审与统编，不直接修改既有稿件（输出批注建议）。
---

# TLAW Book Writer · 书稿写手

> **仓库地址**：<https://github.com/cat-xierluo/tlaw-book-writer>（获取最新版、提 Issue / PR）

> TLAW 为本书项目的内部代号。
>
> **定位**：这个技能不是给人看指引的——它**直接驱动 agent 写稿**。输入一个章节号和手头的素材，产出一节按本书范稿风格、符合写作规范、守住项目口径的成稿；给人看的只有稿末附的一页自查结果。
>
> **技能自包含**：`SKILL.md` + `references/`（standards-checklist / feishu-cli-cookbook）+ `config/project-profile.yaml` 即可运行，不依赖任何外部技能；**lark-cli 为可选增强**（活体校验与写入通道 c），未安装/未授权时静默降级，不阻塞写作与交付。换一本书，替换 references 与 config 即可。

> **配套材料**：本仓不含具体书籍的写作规范全文、范稿与词汇清单（各书自有内容）——按 `config/project-profile.example.yaml` 注释自行配置接入；配置齐全后技能完整生效，未配置时降级运行（跳过风格基线与哨兵）。

## 〇、首次使用引导（profile.author 未配置时执行，一次即成）

1. **查配置**：`config/project-profile.yaml` 不存在，或 `author.name` / `author.sections` 均为空 → 进入引导；已配置则跳过本节。
2. **查飞书连接**：lark-cli 可用且 user ready（自检见 cookbook §0）→ 走第 4 步"库读取+二次确认"；不可用 → 纯问答。
3. **问答采集**（依次问，等回答）：① 您的署名（将出现在文档标题与交稿动作中）？② 您在写的章/小节（如 2.5、2.6；整章可答"第三章"）？
4. **库读取 + 二次确认**（已连飞书时）：按 cookbook §1.5 从飞书多维表格（书稿章节计划）检索该署名对应的章节记录，或从章节节点下的文档标题（"-作者"后缀）推断，把**读到的清单复述给用户确认**："计划表显示您在写 X.Y / X.Z，对吗？需要增减吗？"——**以用户确认为准**；读不到（token 未配 / 权限未开）就退回纯问答，不阻塞。
5. **写入并告知**：把确认后的 name / sections / chapters 写入本地 `project-profile.yaml` 的 author 块（文件不存在则从 example 复制后填），告知"已记住——之后直接说'写我的 X.X'或'我的下一节'"。

## 一、输入

1. **章节号或节标题**（必需）：如"3.4"或"2.6 使用现成连接器"——也可说"我的下一节"，从 profile.author.sections 按目录顺序解析；profile.author 已填时，写不在 sections/chapters 清单内的节须作者显式确认
2. **素材**（可多可少）：作者的材料、要点、口述、旧稿片段、截图说明；没有也可以写（见 §四 事实边界）
3. **作者要求**（可选）：立场、案例偏好、须包含/须避开的内容

## 二、备料三读（写前必做，产物留在 agent 上下文里，不输出给用户）

**1. 读规范**（`references/standards-checklist.md`，现行基线=飞书文档写作与管理所配置的写作规范）→ 提取格式硬约束：四级标题体系（一、→（一）→ 1. →（1），编号手动输入）、**标题样式套用（H1 文章题 / H2 一、/ H3（一）/ H4 1.）**、文档标题 `章号节号 标题-作者`、图名 `章节号-图号-图名` 与**双重提交**（文中居中+题注、原图另传图片目录）、**标注参数正红 1.5 磅**、表格表头表名、字数区间、脱敏、自查清单 8 项。

**2. 读项目口径**（`config/project-profile.yaml`）→ 提取本节口径：
- **节说明**（profile.toc_path 指向的目录中该节的官方说明）——内容的骨架，逐句对应展开
- **违禁词**（禁写/待确认/获准三级）——写前规避，不是写完再扫
- **术语表**（大小写与用法约定）若 token 已录入 profile.feishu，用 lark-cli 活体拉取对照，本地表与远端不一致时以远端为准并提醒更新本地
- **示例材料规则**（虚构或公开，不碰真实案件）
- **文档标题与节点**（`章号节号 标题-作者`）

**2bis. 飞书活体校验（可选，lark-cli 可用时执行）**——按 `references/feishu-cli-cookbook.md`：
- **规范哨兵**：查规范 wiki 的 revision_id 是否 > profile.feishu.revision_baseline；涨了=规范被静默修改，先重拉规范更新 checklist 再写作（基线值在**本地实例** project-profile.yaml 维护；example 模板中为 null 时跳过哨兵）
- 活体拉取规范/术语表最新版，与本地基线比对
- lark-cli 不可用（未安装/未授权）时跳过本步，以本地基线为准，不影响写作

**3. 读范文**（两步：先报告、后原文）→
- **第一步（可选）**读**提取报告**（`profile.extraction_report` 配置的"范文格式·截图·标注提取报告"，随配套材料提供、本仓不含）——体例基线：文字版式、标题编号体系、图名格式与图号纪律、图片尺寸与节奏、截图标注口径、提示词/代码入框、结构框架与语言锚点；若报告带执行清单节，逐条执行。**未配置时跳过本步**，直接进第二步并加大风格提炼力度。
- **第二步**通读 profile.sample_docs 指向的范稿 md 原文，校准**风格指纹**（报告给体例硬指标，原文给语感）：标题句式（任务式命名）、段落节奏（短段、一段一事、图文相间）、案例引入方式（场景→痛点→操作→结果）、语气（"我们"自称、直接陈述）、小结写法。提炼为约 10 条写作时逐条执行的规则。

> 注意：范稿里的产品选材（前作的模型与工具名）**不学**——那是本书违禁词；学的只是格式、体例、语言风格。

三读完成后直接进入写作，不向用户复述"我读了什么"。

## 三、写作

1. **骨架 = 节说明**：目录中该节的官方说明逐句对应展开，每个要求点写成实质内容（小节或成段），不一句话带过
2. **格式 = 所配置的写作规范（+ 提取报告，若配置）**：标题用本书体系（手动编号，md 稿 `#`=文章题 / `##`=一、/ `###`=（一）/ `####`=1.，转入飞书时对应套 H1–H4 样式）；截图位写 `【图：章节号-图号-图名——说明文字】` 占位并注明**标注要求**（正红 1.5 磅，红框圈哪里、是否加序号），真机截图由作者补（PNG ≥1200px，文中插入居中+图下题注，**原图另传知识库图片目录**）；**提示词与代码一律入框**（md 稿用代码块或引用块）；真正的数据表须表头表名
3. **结构 = 范文框架**：场景引入 → 方法是什么 → 分步操作（每步：说明 + 提示词框 + 截图）→ 效果展示 → 常见问题/变体 → 小结；操作步骤编号与截图标注序号、图号一一咬合
4. **风格 = 范文语感**：按 §二·3 提炼的规则成文；**作者明确提供自己旧稿要求按其风格时，文风以作者样本优先，格式层仍向规范对齐**（全书要的是格式统一，不是消灭个人印记）
5. **口径写前规避**：违禁词不落笔（含图内文字与标注文字提示）；术语按表；不越级写后面章节内容，需要提及时一句话带过并交叉引用
6. **篇幅**：profile 字数区间（本书约 3000 字）；写不足先加实质内容（步骤、例子、例外情形），不注水

## 四、事实边界

1. **不编造**：法条条号、案例案号、数字、产品功能——素材里没有的一律不落为事实，需要时写"【需核实：……】"或"【待作者补充：……】"占位，继续写其余部分
2. **示例材料**：按 profile.demo_material_rule——虚构样本或公开文书；虚构的明标"虚构"
3. **防幻觉**：涉及"AI 输出需要核对"的场景，把核对动作写进步骤（规范 §一（五）的硬要求）

## 五、内置自查（成稿后、交付前，对自己刚写的稿跑）

按 `references/standards-checklist.md` 的 L1 机械项（文档标题、标题层级与样式、图名表名与双重提交、字数、违禁词、脱敏、术语）+ 提取报告执行清单（若配置） + 节说明覆盖逐点核对 → 在稿末附一页**自查结果**（每项 PASS / WARN / FAIL + 位置），作者过目后可删。自查发现的问题能改直接改掉再交付；改不了的（如需真机截图）在自查页标明。

## 六、交付（三通道）

1. 成稿 md（含稿末自查页）
2. 按所配置的写作规范 给出**文档标题**：`章号节号 标题-作者`——作者名取 profile.author.name（格式与版本规则依所配置的写作规范；示例见 profile.naming_example）
3. 进入知识库，按 profile.feishu.delivery_channel 选通道（默认 a）：
   - **通道 a · 手动粘贴（默认，零依赖）**：作者打开知识库对应章节节点新建文档，md 全文粘贴（飞书自动把 md 标题转为 H1–H4；核对样式，勿用有序列表自动编号）
   - **通道 b · 请他人代转**：md 转 Word 后请协作者或编辑侧帮忙转入飞书
   - **通道 c · lark-cli 写入（进阶，需授权+已验证）**：按 `references/feishu-cli-cookbook.md` §3——在章节父节点下创建 docx、按 md→块映射写入正文（标题块/段落/代码块/引用块）、图片走媒体上传后插图块。**首次使用前必须先在知识库建一篇测试文档验证权限与块结构，验证通过才可用于正式章节**
4. 交付收尾（所有通道相同）：截图真机补拍（PNG ≥1200px，标注正红 1.5 磅）替换图位——文中居中+图下题注，**原图上传"图片原图"目录且文件名与图名一致**；字体字号不用管（最终排版由导出 Word 的后续流程统一处理）；在自己的进度/计划表更新状态；评论处理完毕再交

## 七、纪律

- 只写指定/配置中声明的节（profile.author.sections/chapters 为章节清单，清单外需显式确认）；协作场景下不直接改动他人稿件，输出批注建议式意见；**API（lark-cli）同样不得用于修改他人文档**——只用于读、建自己的文档
- 规范与 profile 冲突以 profile 为准；profile 升版（目录/规范/范稿/提取报告变化）先改 profile 与 checklist 再写作——规范升版与哨兵报警时同
- 质量深审与统编不在本技能（按你的出版流程另行安排）
- 不生产"写作须知""指引文档"之类的中间产物——所有约束内化为写作行为
- lark-cli 写入是可选通道：不可用或未验证时静默降级到通道 a/b，不阻塞交付

## 八、参考文档

- **范文提取报告（可选，随配套材料提供、本仓不含）**——路径由 `profile.extraction_report` 配置；内容为范稿的格式·截图·标注实测基线与写作执行清单
- [references/standards-checklist.md](references/standards-checklist.md) — 规范的可执行检查表（现行基线版本见 profile.standards_version；写作约束与自查两用）
- [references/feishu-cli-cookbook.md](references/feishu-cli-cookbook.md) — **飞书 CLI 操作手册**（活体读取/规范哨兵/文档创建与块写入/图片上传；写入链路标注验证状态）
- [config/project-profile.example.yaml](config/project-profile.example.yaml) — **配置模板（无任何真实 token 与个人配置）**；使用时复制为 `config/project-profile.yaml` 本地实例（.gitignore 排除），环境字段（文档路径 / 飞书 token / 哨兵基线 / 作者章节）只写在本地实例
